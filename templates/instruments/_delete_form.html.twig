<form method="post" action="{{ path('app_instruments_delete', {'idInstrument': instrument.idInstrument}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ instrument.idInstrument) }}">
    <button class="btn">Delete</button>
</form>

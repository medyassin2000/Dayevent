<form method="post" action="{{ path('app_commande_ticket_deleteF', {'id': commande_ticket.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ commande_ticket.id) }}">
    <button class="btn">Delete</button>
</form>

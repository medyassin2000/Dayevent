{% extends 'back.html.twig' %}

{% block title %}CategorieInstrument{% endblock %}

{% block body %}
    <h1>CategorieInstrument</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ categorie_instrument.id }}</td>
            </tr>
            <tr>
                <th>NomCategorie</th>
                <td>{{ categorie_instrument.nomCategorie }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ categorie_instrument.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_categorie_instrument_index') }}">back to list</a>

    <a href="{{ path('app_categorie_instrument_edit', {'id': categorie_instrument.id}) }}">edit</a>

    {{ include('categorie_instrument/_delete_form.html.twig') }}
{% endblock %}

<div style="margin-top:150px;">

</div>

<section class="vh-100">
  <div class="container py-5 h-100">
    <div class="row d-flex align-items-center justify-content-center h-100">
      <div class="col-md-8 col-lg-7 col-xl-6">
        <img src="{{asset('img/evn.PNG')}}" alt="" class="img-fluid">
      </div>
      
      <div class="col-md-7 col-lg-5 col-xl-5 offset-xl-1">
        {{ form_start(connexion, {'attr': {'class': 'form login-form'}}) }}
        <!-- Email input -->
        {{ form_row(connexion.email, {'attr': {'class': 'form-control form-control-lg'}, 'label': 'Email'}) }}

        <!-- Password input -->
        {{ form_row(connexion.motDePasse, {'attr': {'class': 'form-control form-control-lg'}, 'label': 'Mot de passe'}) }}
   
         
        <div class="d-flex justify-content-around align-items-center mb-4">
          <!-- Checkbox -->
          <div class="form-check">
            
          </div>
          <a href="{{ path('forget') }}">Forgot password?</a>

        </div>

        <!-- Submit button -->
        <input type="submit" class="btn btn-primary btn-lg btn-block" value="login">

        <div class="divider d-flex align-items-center my-4">
          <p class="text-center fw-bold mx-3 mb-0 text-muted">OR</p>
        </div>

        <a class="btn btn-primary btn-lg btn-block" style="background-color: #3b5998" href="{{ path('app_register') }}"
                   role="button">
          <i class=""></i>Sign up</a>
        
        

        {{ form_end(connexion) }}
      </div>
    </div>
  </div>
</section>

<div style="margin-bottom:800px;">
</div>



